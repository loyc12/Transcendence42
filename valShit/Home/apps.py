""" This file is used to configure the home app."""
from django.apps import AppConfig

class HomeConfig(AppConfig):
    """ This class is used to configure the home app."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "Home"
